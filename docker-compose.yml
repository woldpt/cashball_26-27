version: '3.8'
services:
  backend:
    build: ./server
    #    ports:
    #      - "3000:3000"
    volumes:
      - ./server/db:/app/db

  frontend:
    build: ./client
    #    ports:
    #      - "5173:80"
    environment:
      - VITE_BACKEND_URL=http://localhost:3000
    networks:
      cftunnel:
        ipv4_address: 172.100.0.57

networks:
  cftunnel:
    external: true
